{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python -m pip install -r ../requirements.txt",
    "watchPatterns": [
      "/backend/**",
      "/worker/**",
      "/shared/**",
      "/scripts/**",
      "/requirements.txt"
    ]
  },
  "deploy": {
    "startCommand": "bash ../scripts/railway/start-worker-with-beat.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "runtime": "V2"
  }
}
