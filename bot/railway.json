{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python -m pip install -r requirements.txt",
    "watchPatterns": [
      "/backend/**",
      "/bot/**",
      "/shared/**",
      "/locales/**",
      "/requirements.txt"
    ]
  },
  "deploy": {
    "startCommand": "sh scripts/railway/start-bot.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "runtime": "V2"
  }
}
